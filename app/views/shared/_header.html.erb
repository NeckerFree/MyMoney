<div class="d-flex justify-content-between">
  <nav class="navbar navbar-expand-lg">
    <ul class="navigation justify-content-between list-unstyled">
      <li class="nav-li"> <%= link_to "Tradings", tradings_path, class: "link link-invent" %></li>
      <li class="nav-li"> <%= link_to "Categories", categories_path, class: "link link-invent"%></li>
    </ul>
  </nav>
  <ul class="login-bar list-unstyled">
  <% if user_signed_in? %>
  <li>
        <%= button_to "Sign Out", destroy_user_session_path, class: "sign-btn", data: { turbo_method: :delete } %>
      </li>
     <% else %>
      <li>
        <%= link_to "Sign In", new_user_session_path, class: "sign-btn" %>
        <%= link_to "Sign Up", new_user_registration_path, class: "sign-btn" %>
       </li>
    <% end %>
  </ul>
</div>

